const PROXY_CONNECTION_HEADER = 'proxy-connection';
const CONTENT_TYPE = {
    HTML: 'text/html',
    JSON: 'application/json',
};

const PORT = 80;

export { PROXY_CONNECTION_HEADER, CONTENT_TYPE, PORT };
